import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="bg-primary text-white text-center p-4">
            <h1>Мій MERN Проект</h1>
        </header>
    );
};

export default Header;